"""WASD manual-control movement mode.

Modules:
  drive — raw-keyboard WASD/QE teleop + camera servos, with keys to run a 360
          scan (R), build the cloud (T), view it (Y), or single-capture (V).
"""
